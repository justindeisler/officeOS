# Personal Assistant API — Environment Configuration
# Copy this to .env and fill in your values.
# NEVER commit the actual .env file — only this .env.example.

# JWT Secret for authentication (REQUIRED)
# Generate with: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# CRITICAL: Use a strong random value (32+ chars). Server will refuse to start without this.
JWT_SECRET=

# Server port (optional, defaults to 3001)
PORT=3001

# User credentials (bcrypt hashes)
# Generate hash: node -e "console.log(require('bcryptjs').hashSync('YOUR_PASSWORD', 10))"
AUTH_JUSTIN_HASH=$2b$10$...your-hash-here...
AUTH_JAMES_HASH=$2b$10$...your-hash-here...

# User display names
AUTH_JUSTIN_NAME=Justin Deisler
AUTH_JAMES_NAME=James (AI Assistant)

# Optional: Azure OCR (for document scanning)
# AZURE_ENDPOINT=https://your-instance.cognitiveservices.azure.com
# AZURE_API_KEY=your-azure-api-key

# Optional: Static files path (web build)
# STATIC_PATH=/path/to/web/dist
